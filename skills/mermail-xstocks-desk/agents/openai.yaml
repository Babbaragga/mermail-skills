interface:
  display_name: "Mermail xStocks Desk"
  short_description: "Search and preview one controlled Solana xStocks purchase"
  default_prompt: "Use $mermail-xstocks-desk to resolve one evidence-backed product, prepare a USDC purchase with my eligible Solana wallet, and give me one Mermail Agent Wallet review link. Submit only after that browser page records my approval, then use the status tool to reconcile the same purchase."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
