interface:
  display_name: "Mermail xStocks Desk"
  short_description: "Search and preview one controlled Solana xStocks purchase"
  default_prompt: "Use $mermail-xstocks-desk to search the evidence-backed catalog, let me select one product, create an exact short-lived USDC purchase preview, and wait for my explicit approval. Do not use DCA or a generic swap fallback."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
